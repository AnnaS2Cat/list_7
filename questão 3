/*Crie uma função preenchida que receba um array bidimensional arrde inteiros com uma dimensão fixa ( LxC), e preencha o array com l*cnúmeros lidos do teclado, ex:
void preenche (int arr[L][C], int l, int c);
Crie uma função mediaque recebe um array bidimensional arr, a quantidade de linhas l, a quantidade de colunas c, e retorna a média entre todos os valores do array, ex:
float media (int arr[L][C], int l, int c);
L e C devem ser quaisquer valores constantes predefinidos.*/

#include <stdio.h>
#define L 3 
#define C 4
void preenche (int arr[L][C], int l, int c){
    int i,j;
    for(i=0;i<l;i++){
        for(j=0;j<c;j++){
            printf("Digite o valor para arr[%d][%d]: ", i, j);
            scanf("%d", &arr[i][j]);
        }
    }
}
float media(int arr[L][C], int l, int c) {
    int i, j, cont=0 , soma = 0;
    for(i = 0; i<l; i++) {
        for(j = 0; j<c; j++) {
            soma=soma+ arr[i][j];//estou passando pelos elementos de todas as linhas e somando pra botar na média
            cont=cont+1;
        }
    }
    float m= (float)soma/cont; //o float faz a media ter ,
    return m;
}
int main(void) {
    int matriz[L][C];
    preenche(matriz, L, C);
    printf("A média dos elementos da matriz é:%f \n", media(matriz, L, C));
}
